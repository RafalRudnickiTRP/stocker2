<Window x:Class="WpfApplication3.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApplication3"
        mc:Ignorable="d"
        d:DesignWidth="723.2" d:DesignHeight="506.4"
        Title="MainWindow" 
        MinHeight="200" MinWidth="200" >
    <DockPanel>
        <ToolBarTray DockPanel.Dock="Top">
            <ToolBar>
                <Button Command="New" Content="New" />
                <Button Command="Open" Content="Open" />
                <Button Command="Save" Content="Save" />
            </ToolBar>
        </ToolBarTray>
        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem>
                <TextBlock Name="lblCursorPosition" />
            </StatusBarItem>
        </StatusBar>
        <DockPanel DockPanel.Dock="Left" 
                   Width="150" 
                   VerticalAlignment="Top" 
                   LastChildFill="True">
            <Label DockPanel.Dock="Top">group:</Label>
            <ComboBox DockPanel.Dock="Top">
                <ComboBoxItem>uu</ComboBoxItem>
            </ComboBox>
            <Label DockPanel.Dock="Top">filter:</Label>
            <TextBox DockPanel.Dock="Top" 
                     Name="textBox" 
                     Text="..."/>
            <Label DockPanel.Dock="Top">symbols:</Label>
            <ListView Name="SymbolsList" >
                <ListView.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="show" />
                    </ContextMenu>
                </ListView.ContextMenu>
                <ListViewItem>fake</ListViewItem>
            </ListView>
        </DockPanel>
        <TabControl>
            <TabItem Name="tab" Header="Symbol">
                <Canvas Name="canvas">
                    <Rectangle Fill="#FFF4F4F5" Height="100" Canvas.Left="67" Stroke="Black" Canvas.Top="58" Width="100"/>
                </Canvas>
            </TabItem>
        </TabControl>
    </DockPanel>
</Window>
